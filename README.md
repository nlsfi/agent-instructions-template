# Copier templates for AI agents in National Land Survey repositories

## Development environment

Create a virtual environment and install `pre-commit`:

```powershell
pip install pre-commit
pre-commit install
```
